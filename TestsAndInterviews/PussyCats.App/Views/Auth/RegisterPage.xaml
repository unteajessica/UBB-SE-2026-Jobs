<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PussyCats_App.Views.Auth.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="40">
        <StackPanel Width="360" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
            <TextBlock Text="Create account" Style="{StaticResource HeaderTextBlockStyle}" HorizontalAlignment="Center"/>

            <TextBox Header="First Name" Text="{Binding FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Header="Last Name" Text="{Binding LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Header="Email" Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" InputScope="EmailSmtpAddress"/>
            <PasswordBox x:Name="PasswordBox" Header="Password" PasswordChanged="PasswordBox_PasswordChanged"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Header="Confirm password" PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>

            <InfoBar
                IsOpen="True"
                Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                Severity="Error"
                Message="{Binding ErrorMessage}"/>

            <Button
                Content="Register"
                HorizontalAlignment="Stretch"
                Style="{StaticResource AccentButtonStyle}"
                Command="{Binding RegisterCommand}"
                IsEnabled="{Binding IsBusy, Converter={StaticResource BoolNegationConverter}}"/>

            <HyperlinkButton
                Content="Already have an account? Sign in"
                HorizontalAlignment="Center"
                Click="NavigateToLogin_Click"/>
        </StackPanel>
    </Grid>
</Page>
