<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PussyCats_App.Views.Candidate.UserStatusJobDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#F0F2F5">

    <Grid Padding="32,24" RowDefinitions="Auto,*">
        <Grid Grid.Row="0" ColumnDefinitions="Auto,*" Margin="0,0,0,18">
            <Button Content="← Close" Click="Close_Click" VerticalAlignment="Center" Foreground="Black"/>
            <TextBlock Grid.Column="1" x:Name="PageTitleText"
                       FontSize="22" FontWeight="Bold"
                       HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
        </Grid>

        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="14" MaxWidth="840">
                <Border Background="White" CornerRadius="10" Padding="20" BorderBrush="#E1E1E1" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock x:Name="CompanyText"     FontSize="16" FontWeight="SemiBold" Foreground="Black"/>
                        <TextBlock x:Name="ScoreText"       FontSize="13" Foreground="#1565C0" FontWeight="SemiBold"/>
                        <TextBlock x:Name="DescriptionText" TextWrapping="Wrap" Foreground="Black"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
