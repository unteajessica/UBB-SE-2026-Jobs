<Page
    x:Class="Tests_and_Interviews.Views.OurJobsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tests_and_Interviews.Views"
    xmlns:models="using:Tests_and_Interviews.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Back Button -->
        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <Button Content="Back" HorizontalAlignment="Left" Click="BackButton_Click" />
            <TextBlock Text="Our Jobs" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Jobs List -->
        <ListView Grid.Row="1" 
                  ItemsSource="{x:Bind ViewModel.Jobs}" 
                  Visibility="{x:Bind ViewModel.JobsVisibility, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:JobPosting">
                    <Grid Padding="10" Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind JobTitle}"/>
                            <TextBlock Text="{x:Bind JobType}"/>
                        </StackPanel>

                        <!-- Action Buttons -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                            <Button Content="Pay to Promote" 
                                    Click="PayToPromoteButton_Click" 
                                    Tag="{x:Bind}"/>

                            <Button Content="View Applicants" 
                                    Click="ViewApplicantsButton_Click" 
                                    Tag="{x:Bind}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Grid.Row="2"
                Content="Create job"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Margin="0,16,0,0"
                Click="CreateJobButton_Click"/>
    </Grid>
</Page>