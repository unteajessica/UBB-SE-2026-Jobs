<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Tests_and_Interviews.Views.InterviewCandidatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tests_and_Interviews.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border x:Name="RecordingBorder" BorderThickness="10" BorderBrush="Red" >

        <Grid Background="#C9BEFF">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" RowSpacing="8" Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" MaxHeight="750"/>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <MediaPlayerElement x:Name="captureElement" Grid.Row="0" Stretch="UniformToFill" AutoPlay="True"/>


                <Grid Grid.Row="1" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="StartVideoButton" Grid.Column="0" Click="StartRecording_Click" Foreground="#FFDBFD" Background="#8494FF" HorizontalAlignment="Center" Padding="8,12" CornerRadius="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="Video" />
                            <TextBlock Text="Start Video" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="StopVideoButton" Grid.Column="1" Click="StopRecording_Click" Foreground="Red" Background="#8494FF" HorizontalAlignment="Center" Padding="8,12" CornerRadius="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="Stop" />
                            <TextBlock Text="Stop Video" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="SubmitVideoButton" Grid.Column="2" Command="{x:Bind ViewModel.SubmitRecordingCommand}" Foreground="#FFDBFD" Background="#8494FF" HorizontalAlignment="Center" Padding="8,12" CornerRadius="8">
                        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                            <SymbolIcon Symbol="Send" />
                            <TextBlock Text="Submit Video" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                    <Button Grid.Column="3" Click="ExitPage_Click" Foreground="#FFDBFD" Background="#8494FF" HorizontalAlignment="Center" Padding="8,12" CornerRadius="8">
                        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                            <SymbolIcon Symbol="Cancel" />
                            <TextBlock Text="Exit" FontWeight="SemiBold" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>

            <Grid Grid.Column="1" RowSpacing="8" Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" MaxHeight="200"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border 
            Background="#8494FF" 
            BorderBrush="#FFDBFD" 
            BorderThickness="2" 
            CornerRadius="8" 
            Padding="12"                
            Grid.Row="0">

                    <TextBlock 
                Text="{x:Bind ViewModel.QuestionText, Mode=OneWay}"
                Foreground="#FFDBFD" 
                FontSize="24"
                TextWrapping="Wrap"/>
                </Border>

                <Button x:Name="NextQuestionButton" Grid.Row="1" Grid.Column="3" Command="{x:Bind ViewModel.NextQuestionCommand}" Foreground="#FFDBFD" Background="#8494FF" HorizontalAlignment="Right" Padding="24,12" CornerRadius="8">
                    <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                        <TextBlock Text="Next Question" FontWeight="SemiBold" />
                        <SymbolIcon Symbol="Forward" />
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>

    </Border>

    
</Page>