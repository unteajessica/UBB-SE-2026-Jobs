SD_[Actor]_[Action].png
SD_[Actor]_[Action].puml
```

- **SD** prefix is mandatory (stands for Sequence Diagram)
- **Actor** — the role performing the action: `Recruiter`, `Candidate`, `Admin`
- **Action** — a short PascalCase description of the scenario