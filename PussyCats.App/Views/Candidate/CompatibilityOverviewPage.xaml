<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PussyCats_App.Views.Candidate.CompatibilityOverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:PussyCats.App.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Role Compatibility"
                   FontSize="24" FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <ProgressRing Grid.Row="1" IsActive="{x:Bind viewModel.IsLoading, Mode=OneWay}"
                      Width="40" Height="40" HorizontalAlignment="Left" Margin="0,0,0,12"/>

        <ListView x:Name="rolesList" Grid.Row="2" SelectionChanged="RolesList_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:CompatibilityDisplayItem">
                    <Grid Margin="0,8,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind DisplayName}" VerticalAlignment="Center"/>
                        <ProgressBar Grid.Column="1" Minimum="0" Maximum="100"
                                     Value="{x:Bind DisplayScore}" Margin="12,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{x:Bind DisplayPercentage}"
                                   VerticalAlignment="Center" HorizontalAlignment="Right"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
