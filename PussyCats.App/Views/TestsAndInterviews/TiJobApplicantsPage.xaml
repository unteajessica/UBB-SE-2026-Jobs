<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PussyCats_App.Views.TestsAndInterviews.TiJobApplicantsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="← Back" Click="Back_Click" Margin="0,0,16,0"
                    Background="Transparent" BorderThickness="0"/>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Applicants" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="{x:Bind ViewModel.CurrentJob.JobTitle, Mode=OneWay}" FontSize="14"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          Width="48" Height="48" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="0">
                    <!-- Table header -->
                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          Padding="16,12" CornerRadius="8,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="User ID" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" Text="Status" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="2" Text="App Grade" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="3" Text="CV Grade" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="4" Text="Interview" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="5" Text="Applied" FontWeight="SemiBold"/>
                    </Grid>

                    <ListView ItemsSource="{x:Bind ViewModel.Applicants, Mode=OneWay}"
                              SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="16,10" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding UserId}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding ApplicationStatus}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding AppTestGrade}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{Binding CvGrade}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="4" Text="{Binding InterviewGrade}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="5" Text="{Binding AppliedAt}" VerticalAlignment="Center" FontSize="12"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
