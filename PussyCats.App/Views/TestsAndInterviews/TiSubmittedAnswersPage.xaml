<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PussyCats_App.Views.TestsAndInterviews.TiSubmittedAnswersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="← Back" Click="Back_Click" Margin="0,0,16,0"
                    Background="Transparent" BorderThickness="0"/>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="Submitted Answers" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="{x:Bind ViewModel.TestTitle, Mode=OneWay}"
                           FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Visibility="{x:Bind ViewModel.TestTitle, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}"/>
            </StackPanel>
        </Grid>

        <!-- Loading -->
        <ProgressRing Grid.Row="1"
                      IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                      Width="40" Height="40" HorizontalAlignment="Center"
                      Margin="0,0,0,16"
                      Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Summary label -->
        <TextBlock Grid.Row="2"
                   Text="Review your answers below. Correct answers earn full points; partial answers earn reduced points."
                   FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap" Margin="0,0,0,16"
                   Visibility="{x:Bind ViewModel.ReviewItems, Mode=OneWay, Converter={StaticResource ListToVisibilityConverter}}"/>

        <!-- Answer list -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.ReviewItems, Mode=OneWay}">
                <ItemsRepeater.Layout>
                    <StackLayout Spacing="12"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="12" Padding="20,16">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Question + status row -->
                                <Grid Grid.Row="0" Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding QuestionText}"
                                               FontSize="15" FontWeight="SemiBold"
                                               TextWrapping="Wrap" VerticalAlignment="Center"
                                               Margin="0,0,12,0"/>

                                    <Border Grid.Column="1" Background="{Binding StatusBackground}"
                                            CornerRadius="6" Padding="10,4" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Status}"
                                                   Foreground="{Binding StatusForeground}"
                                                   FontSize="13" FontWeight="SemiBold"/>
                                    </Border>
                                </Grid>

                                <!-- Score row -->
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="4" Margin="0,0,0,8">
                                    <TextBlock Text="Score:" FontSize="13"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock FontSize="13" FontWeight="SemiBold">
                                        <Run Text="{Binding EarnedScoreDisplay}"/>
                                        <Run Text="/"/>
                                        <Run Text="{Binding MaxScoreDisplay}"/>
                                    </TextBlock>
                                </StackPanel>

                                <!-- Correct answer row -->
                                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="4" Margin="0,0,0,4">
                                    <TextBlock Text="Correct answer:" FontSize="13"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{Binding CorrectAnswerDisplay}"
                                               FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                </StackPanel>

                                <!-- User answer row (hidden when correct) -->
                                <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="4"
                                            Visibility="{Binding ShowUserAnswer}">
                                    <TextBlock Text="Your answer:" FontSize="13"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{Binding UserAnswerDisplay}"
                                               FontSize="13" TextWrapping="Wrap"/>
                                </StackPanel>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
